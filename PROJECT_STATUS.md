# AI中级会计助手 项目状态（最小 MVP 版）

## 项目概述
微信小程序 + NestJS + PostgreSQL 的会计刷题学习平台，已裁剪为最小 MVP 闭环：
**登录 → 刷题 → 后端判分 → 答题记录 → 自动收集错题 → 错题复习 → 学习统计**。

## 当前版本（2026-09-14 裁剪）

### 保留的模块（MVP 闭环核心）

| 模块 | 后端 | 前端页面 |
|------|------|----------|
| 认证登录 | auth（微信登录 + 游客模式 + 账密登录） | login |
| 用户 | user（资料 + 学习统计） | profile |
| 知识点 | knowledge（章节分类） | -（练习页内嵌） |
| 题库 | question（每日题/章节题/答题判分/解析/搜索） | practice |
| 学习记录 | study（答题流水 + 错题本） | wrong |
| 微信服务 | wechat | - |

### 已移除的功能
- AI 答疑（OpenAI）、拍照识题（腾讯云 OCR）
- 考试模拟、思维导图、语音 TTS、数据分析模块
- 学习计划（study_plans 表及接口）
- Redis 队列（BullMQ）及依赖（openai / bull / redis）

### 数据库（5 张表）
users、knowledge_points、questions、study_records（答题流水）、wrong_questions（错题本）
- 字段命名与 TypeORM 实体完全对齐（修复了原版实体与建表脚本不一致导致查询报错的问题）
- 附种子数据：10 个知识点 + 4 道示例题

### 闭环修复项（原版为 stub，本次补齐）
1. 提交答案接口原本只更新题目统计 → 现为：后端判分 + 存答题流水 + 答错自动收错题 + 更新用户统计
2. 错题接口原本返回空数组 → 现读取错题本真实数据（含题目信息）
3. study 模块原本未挂载 controller/service → 现已注册并提供错题本/今日状态接口
4. users.openid 原为 NOT NULL（普通注册会失败）→ 已允许为空

## 验证状态（2026-09-14 联调完成）
- 后端 `npm run build` 编译通过（TypeScript 无错误）
- **数据库已初始化并通过真实联调**：PostgreSQL 16 本机环境，5 张表 + 种子数据已建库执行
- **完整闭环 API 联调通过**：
  - 游客/微信登录（mock）→ 每日题目 → 四种题型判分（单选/多选/判断/计算）全部正确
  - 答题流水落库、答错自动收错题（重复答错次数累加）、错题本/标记掌握/删除全部正常
  - 今日状态、用户统计、知识点列表、章节练习接口全部正常
- 前端所有 JS 通过 node 语法检查，WXML 事件绑定与 JS 方法一一对应
- 未验证：小程序真机/开发者工具运行（需微信开发者工具打开 front-end 目录）

### 联调中发现并修复的问题
1. JWT 密钥不一致（AuthModule 局部注册用默认 secret 签发，验证用 .env secret）→ 移除多余局部注册
2. 随机题目查询 `ORDER BY RANDOM()` + join 在 PostgreSQL 下报 42P10 → 去掉关联查询
3. 判分正则把纯字母答案 `D` 剥成空串导致全部判错 → 重写规范化逻辑
4. `lastStudyDate` DATE 列返回字符串，调用 `toISOString()` 报错 → 按字符串处理

## 快速开始
见 README.md：初始化数据库 → `npm run start:dev` → 微信开发者工具打开 front-end
